문제!
1. 이 문제의 핵심은 순열과 소수찾기야
2. 그리고 Testcase 011부분에서 나올 수 있는 경우인 011 11은 같은 수로 한번만 count를 추가해주는 과정이 필요해. (이 문제의 살짝 어려운 이유였지.)

차이점!
1. 순열 돌리는 과정이 달랐어. 나는 기존의 배열을 swap으로 변형 시키고 다시 백트랙킹하는 과정을 이용했고
이 친구는 substring으로 String값의 index를 이용해서 순환 시켜줬지.
2. 소수를 찾는 과정에서도 나는 2보다 큰지 확인한 후, 값을 2로나눈 값까지 돌리면서 소수인지 확인했지만,
이 친구는 일단 2를 제외시키고 홀 수만 데리고 제곱근까지 for문을 돌리면서 소수를 확인했어.
3. 문제 2번의 경우를 나는 overlab이라는 함수를 이용해서 중복된 내용이 들어올경우,  stack에 추가하지 않음으로 똑같은 수를 제외시켰지만,
이친구는 hashset이라는 알아서 걸러주는 자료구조를 이용해서 문제를 해결했어.