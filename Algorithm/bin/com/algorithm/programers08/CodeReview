1.다른 사람의 코드는 총 걸리는 기간을 처음부터 리스트에 저장했어.
	->이 과정은 실제 알고리즘이 돌아가는 시점에서 내가 add함수를 호출하여 for문을 여러번 돌리는 과정을 최소화 시켯지.
	
2. 잉 ? 리뷰한 코드가 이상한데 ?
	-> Testcase(걸리는 시간을 기준으로) 5 3 7 4 8  로 넣었을 경우에 원래 정답은 3(5일),1(7일),1(8일)
	이어야 하는데  2,2,1이 나왔어.. 뭔가 이상해...
3. 하지만 걸리는 기간을 리스트에 담아서 보내므로 시간을 단축한건 사실!