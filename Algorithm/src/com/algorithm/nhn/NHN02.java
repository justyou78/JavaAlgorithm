package com.algorithm.nhn;

import java.util.ArrayList;
import java.util.Scanner;



/*
 * class Main { public static void main(String[] args) throws Exception {
 * Scanner sc = new Scanner(System.in);
 * 
 * 2 3개 1 2개 -> 2하나 빠져 count기준 2 2개 1 2개 1이 먼저옴 Arraylist 만들고 comparable만들고 num
 * cout 만들고 enququ하면 그 인덱스에 값넣어주고 dequeue하면 --해주고 0이면 remove
 * 
 * 
 * 
 * 
 * ArrayList<FQueue> al = new ArrayList<FQueue>();
 * 
 * 
 * 
 * int count = Integer.parseInt(sc.nextLine()); for (int i = 0; i < count; i++)
 * { String st = sc.nextLine(); String command[] = st.split(" ");
 * 
 * if(command[0].equals("enqueue")) { al.add(new FQueue(command[1], count,
 * index)) } else {
 * 
 * }
 * 
 * 
 * }
 * 
 * 
 * } class FQueue implements Comparable<FQueue>{ int num; int count; int index;
 * FQueue(int num, int count, int index){ this.num = num; this.count =count;
 * this.index = index; }
 * 
 * @Override public int compareTo(FQueue o) { if(this.count < o.count) { return
 * 1; } else if(this.count == o.count) { if(this.index >o.index) { return 1; } }
 * return -1;
 * 
 * 
 * }
 * 
 * } }
 */